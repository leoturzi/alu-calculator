# Disable lifecycle scripts (preinstall, postinstall, prepare, etc.) on all
# packages to prevent malicious code from running on dependency install.
# Husky hooks are committed directly in .husky/ so they don't need `prepare`.
ignore-scripts=true
